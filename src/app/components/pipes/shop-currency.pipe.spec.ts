import { ShopCurrencyPipe } from './shop-currency.pipe';

describe('ShopCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
